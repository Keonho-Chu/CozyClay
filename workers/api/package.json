{
  "name": "@cozyclay/demo-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --env development",
    "deploy": "wrangler deploy --config wrangler.toml",
    "migrate:local": "wrangler d1 migrations apply cozyclay-demo --local --config wrangler.toml --env development",
    "migrate:remote": "wrangler d1 migrations apply cozyclay-demo --remote --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
